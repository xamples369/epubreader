# Phase 1 — M2: Library + DB + Settings Backbone Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Postaviť dátový backbone (Drift DB + DAOs + EPUB parser + Riverpod state) a minimálnu funkčnú knižnicu, kde sa dá pridať EPUB súbor a kliknutím otvoriť (zatiaľ prázdny) Reader stub.

**Architecture:** Feature-first organizácia (`lib/features/<feature>/`), čistá doménová vrstva bez Flutter závislostí (`lib/domain/`), data vrstva s Drift DB + EPUB parser + cover storage (`lib/data/`). Riverpod ako state container, `epub_view` ako EPUB parser (rovnaký balík ktorý zvolíme aj v M3 na rendering, takže netreba ďalší). Všetky UI stringy ihneď v ARB súbore — zero hardcoded UI text.

**Tech Stack:** Drift ^2.x (SQLite ORM s code generation), `sqlite3_flutter_libs`, `flutter_riverpod` ^2.x, `riverpod_annotation` + `riverpod_generator` (code gen pre providery), `build_runner`, `epub_view` (znovu — ako parser), `path_provider` (už máme), `flutter_localizations` + `intl` + `gen-l10n`.

---

## Súborová mapa (cieľový stav po M2)

```
lib/
├── main.dart                         (modify — len bootstrap, MaterialApp ide do app.dart)
├── app.dart                          (CREATE — root MaterialApp s lokalizáciou)
├── l10n/
│   ├── app_sk.arb                    (CREATE — primárny preklad)
│   └── app_en.arb                    (CREATE — kostra na neskoršie preklady)
├── data/
│   ├── database/
│   │   ├── app_database.dart         (CREATE — Drift @DriftDatabase trieda)
│   │   ├── app_database.g.dart       (generated by build_runner)
│   │   ├── connection.dart           (CREATE — platformový builder Drift connection)
│   │   ├── tables/
│   │   │   ├── books_table.dart      (CREATE)
│   │   │   ├── reading_positions_table.dart  (CREATE)
│   │   │   ├── watched_folders_table.dart    (CREATE — schéma pripravená, UI neskôr)
│   │   │   └── settings_kv_table.dart        (CREATE)
│   │   └── daos/
│   │       ├── books_dao.dart        (CREATE)
│   │       └── settings_kv_dao.dart  (CREATE)
│   ├── epub/
│   │   └── epub_metadata_parser.dart (CREATE — read title/author/cover bytes)
│   └── storage/
│       └── cover_storage.dart        (CREATE — writes covers to ApplicationSupportDir)
├── domain/
│   └── models/
│       └── book.dart                 (CREATE — immutable doménový Book)
└── features/
    ├── library/
    │   ├── add_book_use_case.dart    (CREATE — orchestrácia parse → store → cover)
    │   ├── library_notifier.dart     (CREATE — Riverpod notifier)
    │   ├── library_providers.dart    (CREATE — providery pre DB, dao, use case)
    │   ├── library_screen.dart       (CREATE — grid/list + add button)
    │   └── library_book_card.dart    (CREATE — jeden tile/riadok)
    └── reader/
        └── reader_stub_screen.dart   (CREATE — placeholder s názvom knihy)

test/
├── widget_test.dart                  (modify — nový smoke test pre Library home)
├── data/
│   ├── database/
│   │   ├── books_dao_test.dart       (CREATE — in-memory DB)
│   │   └── settings_kv_dao_test.dart (CREATE — in-memory DB)
│   └── epub/
│       └── epub_metadata_parser_test.dart (CREATE — uses test/fixtures/*.epub)
└── features/
    └── library/
        ├── add_book_use_case_test.dart  (CREATE — mocks DAO + cover storage)
        └── library_notifier_test.dart   (CREATE — mocks use case)
```

**Súbory ktoré sa zmažú** (po Task 17):
- `lib/spike/spike_menu.dart`
- `lib/spike/epub_view_spike.dart`
- `lib/shared/fixture_picker.dart` (nahradíme priamym volaním v Library)
- celý `lib/spike/` aj `lib/shared/` priečinok (ak prázdne)

---

## Predpoklady

- M1 mergnutý na master, branch `m2-library-backbone` vytvorený.
- `epub_view ^3.2.0` v pubspec.
- `file_picker ^11.0.2` v pubspec.
- Flutter scaffold funguje na Windows.

---

## Task 1: Pridať Drift, Riverpod, build_runner

**Files:**
- Modify: `pubspec.yaml`

- [ ] **Step 1.1: Pridať runtime dependencies cez `flutter pub add`**

Run:
```
flutter pub add drift drift_flutter sqlite3_flutter_libs flutter_riverpod riverpod_annotation
```

- [ ] **Step 1.2: Pridať dev_dependencies**

Run:
```
flutter pub add --dev build_runner drift_dev riverpod_generator custom_lint riverpod_lint
```

- [ ] **Step 1.3: Verify pub get**

Run: `flutter pub get`

Expected: `Got dependencies!` Žiadne errors. Lockfile sa rozšíri.

- [ ] **Step 1.4: Verify analyze**

Run: `flutter analyze`

Expected: `No issues found!`.

- [ ] **Step 1.5: Commit & push**

```
git add pubspec.yaml pubspec.lock
git commit -m "chore(m2): add drift, riverpod, build_runner dependencies"
git push
```

**No `Co-Authored-By:` trailer.**

---

## Task 2: Lib štruktúra (app.dart, prázdne priečinky)

Vytvoríme `lib/app.dart` a presunieme `MaterialApp` setup z `main.dart`. `main.dart` ostane len `runApp(ProviderScope(child: const EpubReaderApp()))`. Tento task ešte nemení správanie — len pripravuje štruktúru.

**Files:**
- Create: `lib/app.dart`
- Modify: `lib/main.dart`

- [ ] **Step 2.1: Vytvor `lib/app.dart`** s týmto obsahom:

```dart
import 'package:flutter/material.dart';

import 'spike/epub_view_spike.dart';
import 'spike/spike_menu.dart';

class EpubReaderApp extends StatelessWidget {
  const EpubReaderApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'EPUB Reader',
      theme: ThemeData(
        colorScheme: ColorScheme.fromSeed(seedColor: Colors.indigo),
        useMaterial3: true,
      ),
      home: const SpikeMenuScreen(),
      routes: {
        '/spike/epub_view': (_) => const EpubViewSpikeScreen(),
      },
      onUnknownRoute: (settings) => MaterialPageRoute(
        builder: (_) => Scaffold(
          appBar: AppBar(title: const Text('Not implemented')),
          body: Center(child: Text('Route: ${settings.name}')),
        ),
      ),
    );
  }
}
```

- [ ] **Step 2.2: Nahraď `lib/main.dart`** kompletne:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

import 'app.dart';

void main() {
  runApp(const ProviderScope(child: EpubReaderApp()));
}
```

- [ ] **Step 2.3: Spusti test a analyze**

```
flutter analyze
flutter test
```

Expected: oboje pass. Existing widget test bude stále pass (testuje SpikeMenuScreen).

- [ ] **Step 2.4: Commit & push**

```
git add lib/
git commit -m "refactor(m2): move MaterialApp into app.dart, wrap in ProviderScope"
git push
```

---

## Task 3: i18n scaffolding (Slovak ARB)

Pripravíme `flutter_localizations` aby ďalšie UI tasky vedeli použiť `AppLocalizations.of(context)` namiesto hardcoded stringov.

**Files:**
- Create: `lib/l10n/app_sk.arb`
- Create: `lib/l10n/app_en.arb`
- Create: `l10n.yaml` (project root)
- Modify: `pubspec.yaml` (add flutter_localizations + intl + generate flag)
- Modify: `lib/app.dart` (wire localizations)

- [ ] **Step 3.1: Pridať lokalizačné balíky**

Run:
```
flutter pub add intl
```

(`flutter_localizations` je v SDK, nepridáva sa cez `pub add`. Pridáme ho do pubspec.yaml ručne v ďalšom kroku.)

- [ ] **Step 3.2: Edit `pubspec.yaml` — pridať `flutter_localizations` a `generate: true`**

Pod `dependencies:` (nad `intl:`) pridaj:

```yaml
  flutter_localizations:
    sdk: flutter
```

V sekcii `flutter:` (na konci pubspec.yaml) pridaj riadok:

```yaml
flutter:
  uses-material-design: true
  generate: true
```

(Ak tam už `uses-material-design: true` je, len doplň `generate: true` pod ním.)

- [ ] **Step 3.3: Vytvor `l10n.yaml` v koreni projektu**

```yaml
arb-dir: lib/l10n
template-arb-file: app_sk.arb
output-localization-file: app_localizations.dart
output-class: AppLocalizations
synthetic-package: true
```

- [ ] **Step 3.4: Vytvor `lib/l10n/app_sk.arb`**

```json
{
  "@@locale": "sk",
  "appTitle": "EPUB Reader",
  "@appTitle": {
    "description": "Application title in the system task switcher"
  },
  "libraryEmpty": "Knižnica je prázdna. Pridaj prvú knihu.",
  "@libraryEmpty": {},
  "addBook": "Pridať knihu",
  "@addBook": {},
  "libraryTitle": "Knižnica",
  "@libraryTitle": {},
  "viewGrid": "Mriežka",
  "@viewGrid": {},
  "viewList": "Zoznam",
  "@viewList": {},
  "readerStubTitle": "Reader (v príprave)",
  "@readerStubTitle": {},
  "readerStubBody": "Toto je dočasná obrazovka. Reálne čítanie príde v M3.",
  "@readerStubBody": {},
  "errorOpenEpub": "Nepodarilo sa otvoriť EPUB súbor.",
  "@errorOpenEpub": {},
  "unknownAuthor": "Neznámy autor",
  "@unknownAuthor": {}
}
```

- [ ] **Step 3.5: Vytvor `lib/l10n/app_en.arb`** (kostra so slovenskými hodnotami ako fallback — preložia sa neskôr)

```json
{
  "@@locale": "en",
  "appTitle": "EPUB Reader",
  "libraryEmpty": "Your library is empty. Add your first book.",
  "addBook": "Add book",
  "libraryTitle": "Library",
  "viewGrid": "Grid",
  "viewList": "List",
  "readerStubTitle": "Reader (coming soon)",
  "readerStubBody": "This is a placeholder. Real reading lands in M3.",
  "errorOpenEpub": "Could not open EPUB file.",
  "unknownAuthor": "Unknown author"
}
```

- [ ] **Step 3.6: `flutter pub get`** — vygeneruje `AppLocalizations` v `.dart_tool/flutter_gen/gen_l10n/`.

Expected: `Got dependencies!` no errors. Ak `gen-l10n` zlyhal, prečítaj výpis a podľa toho fix-ni `l10n.yaml`.

- [ ] **Step 3.7: Edit `lib/app.dart` — wire localizations**

Pridaj importy navrch:
```dart
import 'package:flutter_localizations/flutter_localizations.dart';
import 'package:flutter_gen/gen_l10n/app_localizations.dart';
```

V `MaterialApp(...)` pridaj parametre (nad `theme:`):

```dart
      localizationsDelegates: AppLocalizations.localizationsDelegates,
      supportedLocales: AppLocalizations.supportedLocales,
```

Zmen `title: 'EPUB Reader'` na `title: 'EPUB Reader'` (zatiaľ statický — title sa nedá lokalizovať priamo cez l10n bez `onGenerateTitle`).

- [ ] **Step 3.8: Verify**

```
flutter analyze
flutter test
```

Expected: pass.

- [ ] **Step 3.9: Commit & push**

```
git add pubspec.yaml pubspec.lock l10n.yaml lib/
git commit -m "feat(m2): wire i18n scaffolding (slovak + english arb)"
git push
```

---

## Task 4: DB tabuľky (Books, ReadingPositions, WatchedFolders, SettingsKv)

Definujeme 4 Drift tabuľky v separate súboroch. Zatiaľ len schémy — DAOs prídu v Task 6-7.

**Files:**
- Create: `lib/data/database/tables/books_table.dart`
- Create: `lib/data/database/tables/reading_positions_table.dart`
- Create: `lib/data/database/tables/watched_folders_table.dart`
- Create: `lib/data/database/tables/settings_kv_table.dart`

- [ ] **Step 4.1: Vytvor `lib/data/database/tables/books_table.dart`**

```dart
import 'package:drift/drift.dart';

class Books extends Table {
  TextColumn get id => text()();
  TextColumn get title => text()();
  TextColumn get author => text().nullable()();
  TextColumn get language => text().nullable()();
  TextColumn get filePath => text()();
  TextColumn get storageMode => text()(); // 'managed' | 'watched'
  TextColumn get coverPath => text().nullable()();
  DateTimeColumn get addedAt => dateTime()();
  DateTimeColumn get lastReadAt => dateTime().nullable()();
  IntColumn get fileSizeBytes => integer().nullable()();

  @override
  Set<Column> get primaryKey => {id};
}
```

- [ ] **Step 4.2: Vytvor `lib/data/database/tables/reading_positions_table.dart`**

```dart
import 'package:drift/drift.dart';

import 'books_table.dart';

class ReadingPositions extends Table {
  TextColumn get bookId => text().references(Books, #id, onDelete: KeyAction.cascade)();
  IntColumn get chapterIndex => integer()();
  RealColumn get progressInChapter => real()(); // 0.0 – 1.0
  DateTimeColumn get updatedAt => dateTime()();

  @override
  Set<Column> get primaryKey => {bookId};
}
```

- [ ] **Step 4.3: Vytvor `lib/data/database/tables/watched_folders_table.dart`**

```dart
import 'package:drift/drift.dart';

class WatchedFolders extends Table {
  IntColumn get id => integer().autoIncrement()();
  TextColumn get path => text()();
  DateTimeColumn get lastScannedAt => dateTime().nullable()();
}
```

- [ ] **Step 4.4: Vytvor `lib/data/database/tables/settings_kv_table.dart`**

```dart
import 'package:drift/drift.dart';

class SettingsKv extends Table {
  TextColumn get key => text()();
  TextColumn get jsonValue => text()();

  @override
  Set<Column> get primaryKey => {key};
}
```

- [ ] **Step 4.5: Verify analyze**

Run: `flutter analyze`

Expected: clean. Tabuľky zatiaľ nikde nepoužité, ale syntakticky valídne.

- [ ] **Step 4.6: Commit & push**

```
git add lib/data/
git commit -m "feat(m2): drift table definitions (books, positions, watched, settings)"
git push
```

---

## Task 5: AppDatabase + connection + build_runner generation

Vytvoríme `AppDatabase` triedu s `@DriftDatabase` annotáciou, helper na platformový opening, a spustíme code generation.

**Files:**
- Create: `lib/data/database/connection.dart`
- Create: `lib/data/database/app_database.dart`
- Generated: `lib/data/database/app_database.g.dart`

- [ ] **Step 5.1: Vytvor `lib/data/database/connection.dart`**

```dart
import 'package:drift_flutter/drift_flutter.dart';

import 'app_database.dart';

/// Otvorí lokálnu SQLite databázu v platform-specific app data dir.
AppDatabase openAppDatabase() {
  return AppDatabase(driftDatabase(name: 'epubreader'));
}
```

- [ ] **Step 5.2: Vytvor `lib/data/database/app_database.dart`**

```dart
import 'package:drift/drift.dart';

import 'tables/books_table.dart';
import 'tables/reading_positions_table.dart';
import 'tables/settings_kv_table.dart';
import 'tables/watched_folders_table.dart';

part 'app_database.g.dart';

@DriftDatabase(
  tables: [Books, ReadingPositions, SettingsKv, WatchedFolders],
)
class AppDatabase extends _$AppDatabase {
  AppDatabase(super.e);

  @override
  int get schemaVersion => 1;
}
```

- [ ] **Step 5.3: Spusti code generation**

Run:
```
dart run build_runner build --delete-conflicting-outputs
```

Expected: vygeneruje sa `lib/data/database/app_database.g.dart`. Žiadne errors. Drift má veľa vygenerovaného kódu — toto je normálne.

- [ ] **Step 5.4: Verify**

```
flutter analyze
flutter test
```

Expected: pass. `app_database.g.dart` je v `.gitignore`? **Nie** — generated Drift code chceme commitnúť pretože nie všetci jeho developeri budú spúšťať build_runner pred prvým buildom. Pokračuj.

- [ ] **Step 5.5: Commit & push**

```
git add lib/data/
git commit -m "feat(m2): AppDatabase + drift code generation"
git push
```

---

## Task 6: BooksDao + test

Vytvoríme DAO s metódami `insert`, `getAll`, `getById`, `deleteById`, `watchAll` (Stream pre Riverpod).

**Files:**
- Create: `lib/data/database/daos/books_dao.dart`
- Create: `test/data/database/books_dao_test.dart`

- [ ] **Step 6.1: Vytvor `test/data/database/books_dao_test.dart`** (test first)

```dart
import 'package:drift/drift.dart';
import 'package:drift/native.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:epubreader/data/database/app_database.dart';
import 'package:epubreader/data/database/daos/books_dao.dart';

void main() {
  late AppDatabase db;
  late BooksDao dao;

  setUp(() {
    db = AppDatabase(DatabaseConnection(NativeDatabase.memory()));
    dao = BooksDao(db);
  });

  tearDown(() async {
    await db.close();
  });

  test('insert + getAll round trip', () async {
    final row = BooksCompanion.insert(
      id: 'b1',
      title: 'Alice',
      filePath: '/tmp/alice.epub',
      storageMode: 'managed',
      addedAt: DateTime(2026, 6, 20),
      author: const Value('Lewis Carroll'),
    );
    await dao.insertBook(row);

    final all = await dao.getAll();
    expect(all, hasLength(1));
    expect(all.single.id, 'b1');
    expect(all.single.title, 'Alice');
    expect(all.single.author, 'Lewis Carroll');
  });

  test('getById returns null for missing id', () async {
    final result = await dao.getById('nope');
    expect(result, isNull);
  });

  test('deleteById removes the book', () async {
    await dao.insertBook(BooksCompanion.insert(
      id: 'b1',
      title: 'A',
      filePath: '/x',
      storageMode: 'managed',
      addedAt: DateTime(2026, 6, 20),
    ));
    await dao.deleteById('b1');
    expect(await dao.getAll(), isEmpty);
  });
}
```

- [ ] **Step 6.2: Vytvor `lib/data/database/daos/books_dao.dart`** (minimal impl)

```dart
import 'package:drift/drift.dart';

import '../app_database.dart';
import '../tables/books_table.dart';

part 'books_dao.g.dart';

@DriftAccessor(tables: [Books])
class BooksDao extends DatabaseAccessor<AppDatabase> with _$BooksDaoMixin {
  BooksDao(super.db);

  Future<void> insertBook(BooksCompanion row) async {
    await into(books).insert(row);
  }

  Future<List<Book>> getAll() async {
    return (select(books)..orderBy([(b) => OrderingTerm.desc(b.addedAt)])).get();
  }

  Future<Book?> getById(String id) async {
    return (select(books)..where((b) => b.id.equals(id))).getSingleOrNull();
  }

  Future<void> deleteById(String id) async {
    await (delete(books)..where((b) => b.id.equals(id))).go();
  }

  Stream<List<Book>> watchAll() {
    return (select(books)..orderBy([(b) => OrderingTerm.desc(b.addedAt)])).watch();
  }
}
```

(`Book` je Drift-generated row class — bude vygenerovaný z `Books` tabuľky.)

- [ ] **Step 6.3: Re-run code generation**

```
dart run build_runner build --delete-conflicting-outputs
```

Expected: pribudne `lib/data/database/daos/books_dao.g.dart`. Žiadne errors.

- [ ] **Step 6.4: Spusti test**

```
flutter test test/data/database/books_dao_test.dart
```

Expected: 3 testy pass.

- [ ] **Step 6.5: Verify analyze + full test suite**

```
flutter analyze
flutter test
```

Expected: oboje pass.

- [ ] **Step 6.6: Commit & push**

```
git add lib/data/ test/data/
git commit -m "feat(m2): BooksDao + in-memory integration tests"
git push
```

---

## Task 7: SettingsKvDao + test

Jednoduchý key-value DAO pre user preferences (storage mode, štartová obrazovka, atď.).

**Files:**
- Create: `lib/data/database/daos/settings_kv_dao.dart`
- Create: `test/data/database/settings_kv_dao_test.dart`

- [ ] **Step 7.1: Vytvor `test/data/database/settings_kv_dao_test.dart`**

```dart
import 'package:drift/drift.dart';
import 'package:drift/native.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:epubreader/data/database/app_database.dart';
import 'package:epubreader/data/database/daos/settings_kv_dao.dart';

void main() {
  late AppDatabase db;
  late SettingsKvDao dao;

  setUp(() {
    db = AppDatabase(DatabaseConnection(NativeDatabase.memory()));
    dao = SettingsKvDao(db);
  });

  tearDown(() async => db.close());

  test('get on missing key returns null', () async {
    expect(await dao.getString('missing'), isNull);
  });

  test('set then get roundtrips a string', () async {
    await dao.setString('storageMode', 'managed');
    expect(await dao.getString('storageMode'), 'managed');
  });

  test('set overwrites existing value', () async {
    await dao.setString('startupScreen', 'home');
    await dao.setString('startupScreen', 'lastBook');
    expect(await dao.getString('startupScreen'), 'lastBook');
  });
}
```

- [ ] **Step 7.2: Vytvor `lib/data/database/daos/settings_kv_dao.dart`**

```dart
import 'package:drift/drift.dart';

import '../app_database.dart';
import '../tables/settings_kv_table.dart';

part 'settings_kv_dao.g.dart';

@DriftAccessor(tables: [SettingsKv])
class SettingsKvDao extends DatabaseAccessor<AppDatabase>
    with _$SettingsKvDaoMixin {
  SettingsKvDao(super.db);

  Future<String?> getString(String key) async {
    final row = await (select(settingsKv)..where((r) => r.key.equals(key)))
        .getSingleOrNull();
    return row?.jsonValue;
  }

  Future<void> setString(String key, String value) async {
    await into(settingsKv).insertOnConflictUpdate(
      SettingsKvCompanion.insert(key: key, jsonValue: value),
    );
  }
}
```

- [ ] **Step 7.3: Re-run code generation**

```
dart run build_runner build --delete-conflicting-outputs
```

- [ ] **Step 7.4: Spusti test**

```
flutter test test/data/database/settings_kv_dao_test.dart
```

Expected: 3 testy pass.

- [ ] **Step 7.5: Verify all**

```
flutter analyze
flutter test
```

- [ ] **Step 7.6: Commit & push**

```
git add lib/data/ test/data/
git commit -m "feat(m2): SettingsKvDao with insert-or-update semantics"
git push
```

---

## Task 8: Domain Book model + test

Drift-generated `Book` row je perfektný pre data layer, ale na vrstvu UI/domain chceme vlastný immutable model. To umožní neskôr meniť DB bez prepisovania UI.

**Files:**
- Create: `lib/domain/models/book.dart`
- Create: `test/domain/models/book_test.dart`

- [ ] **Step 8.1: Vytvor `test/domain/models/book_test.dart`**

```dart
import 'package:flutter_test/flutter_test.dart';

import 'package:epubreader/data/database/app_database.dart';
import 'package:epubreader/domain/models/book.dart';

void main() {
  test('Book.fromRow maps all fields', () {
    final row = Book(
      id: 'id-1',
      title: 'Alice',
      author: 'Carroll',
      language: 'en',
      filePath: '/p/alice.epub',
      storageMode: 'managed',
      coverPath: '/c/alice.png',
      addedAt: DateTime(2026, 6, 20),
      lastReadAt: null,
      fileSizeBytes: 12345,
    );
    final book = BookModel.fromRow(row);
    expect(book.id, 'id-1');
    expect(book.title, 'Alice');
    expect(book.author, 'Carroll');
    expect(book.coverPath, '/c/alice.png');
    expect(book.isUnread, isTrue);
  });

  test('Book without lastReadAt is unread', () {
    final book = BookModel(
      id: 'x',
      title: 't',
      author: null,
      language: null,
      filePath: '/x',
      storageMode: 'managed',
      coverPath: null,
      addedAt: DateTime.now(),
      lastReadAt: null,
      fileSizeBytes: null,
    );
    expect(book.isUnread, isTrue);
  });
}
```

Pozn.: `Book` (Drift row class) a `BookModel` (domain) majú zámerne rôzne mená aby sa nebili. Domain je v `book.dart`, Drift v `app_database.g.dart`.

- [ ] **Step 8.2: Vytvor `lib/domain/models/book.dart`**

```dart
import 'package:meta/meta.dart';

import '../../data/database/app_database.dart';

@immutable
class BookModel {
  final String id;
  final String title;
  final String? author;
  final String? language;
  final String filePath;
  final String storageMode;
  final String? coverPath;
  final DateTime addedAt;
  final DateTime? lastReadAt;
  final int? fileSizeBytes;

  const BookModel({
    required this.id,
    required this.title,
    required this.author,
    required this.language,
    required this.filePath,
    required this.storageMode,
    required this.coverPath,
    required this.addedAt,
    required this.lastReadAt,
    required this.fileSizeBytes,
  });

  bool get isUnread => lastReadAt == null;

  factory BookModel.fromRow(Book row) => BookModel(
        id: row.id,
        title: row.title,
        author: row.author,
        language: row.language,
        filePath: row.filePath,
        storageMode: row.storageMode,
        coverPath: row.coverPath,
        addedAt: row.addedAt,
        lastReadAt: row.lastReadAt,
        fileSizeBytes: row.fileSizeBytes,
      );
}
```

- [ ] **Step 8.3: Pridať `meta` package ak nie je**

`meta` je tranzitív cez `flutter` SDK, ale pre čistý import pridáme priamo:

Run: `flutter pub add meta`

- [ ] **Step 8.4: Spusti test**

```
flutter test test/domain/models/book_test.dart
```

Expected: 2 testy pass.

- [ ] **Step 8.5: Verify all + commit & push**

```
flutter analyze
flutter test
git add lib/domain/ test/domain/ pubspec.yaml pubspec.lock
git commit -m "feat(m2): domain BookModel with fromRow mapping"
git push
```

---

## Task 9: EPUB metadata parser + test

Extrahuje `title`, `author`, `language`, a bytes obálky z EPUB súboru pomocou `epub_view` knižnice (presnejšie jeho `EpubDocument`, ktorý wrapuje epubx parser).

**Files:**
- Create: `lib/data/epub/epub_metadata_parser.dart`
- Create: `test/data/epub/epub_metadata_parser_test.dart`

- [ ] **Step 9.1: Vytvor `test/data/epub/epub_metadata_parser_test.dart`**

```dart
import 'dart:io';

import 'package:flutter_test/flutter_test.dart';

import 'package:epubreader/data/epub/epub_metadata_parser.dart';

void main() {
  test('alice.epub parses title, author, cover bytes', () async {
    final file = File('test/fixtures/alice.epub');
    expect(file.existsSync(), isTrue, reason: 'fixture missing');

    final meta = await EpubMetadataParser.parse(file);

    expect(meta.title, contains('Alice'));
    expect(meta.author, isNotNull);
    expect(meta.coverBytes, isNotNull);
    expect(meta.coverBytes!.length, greaterThan(100));
  });

  test('divina.epub has Italian author + language', () async {
    final file = File('test/fixtures/divina.epub');
    final meta = await EpubMetadataParser.parse(file);

    expect(meta.title, isNotEmpty);
    expect(meta.author, contains('Dante'));
  });

  test('parse throws on invalid file', () async {
    final tmp = File('${Directory.systemTemp.path}/not-an-epub.epub');
    await tmp.writeAsString('not really an epub');
    expect(
      () => EpubMetadataParser.parse(tmp),
      throwsA(isA<Exception>()),
    );
    await tmp.delete();
  });
}
```

- [ ] **Step 9.2: Vytvor `lib/data/epub/epub_metadata_parser.dart`**

```dart
import 'dart:io';
import 'dart:typed_data';

import 'package:epubx/epubx.dart';

class EpubMetadata {
  final String title;
  final String? author;
  final String? language;
  final Uint8List? coverBytes;

  const EpubMetadata({
    required this.title,
    required this.author,
    required this.language,
    required this.coverBytes,
  });
}

class EpubMetadataParser {
  static Future<EpubMetadata> parse(File file) async {
    final bytes = await file.readAsBytes();
    final book = await EpubReader.readBook(bytes);

    final title = book.Title ?? file.uri.pathSegments.last;
    final author = (book.AuthorList?.isNotEmpty ?? false)
        ? book.AuthorList!.first
        : book.Author;

    String? language;
    final langItem = book.Schema?.Package?.Metadata?.Languages?.cast<String?>().firstWhere(
          (l) => l != null && l.isNotEmpty,
          orElse: () => null,
        );
    language = langItem;

    final coverImage = book.CoverImage;
    final coverBytes = coverImage == null
        ? null
        : Uint8List.fromList(_imageToBytes(coverImage));

    return EpubMetadata(
      title: title,
      author: author,
      language: language,
      coverBytes: coverBytes,
    );
  }

  static List<int> _imageToBytes(Image image) {
    // epubx returns image package's Image; encode to PNG for storage.
    return encodePng(image);
  }
}
```

**Pozn.:** `epubx` exportuje `Image` z `image` package. Funkcia `encodePng` je z toho istého package — `import 'package:image/image.dart';`. Pri analyze môže byť potrebné pridať `image` ako priamy dep (`flutter pub add image`) — ak `flutter analyze` reportuje nedostupný symbol, pridaj ho.

- [ ] **Step 9.3: Verify pub-add image ak treba**

Run:
```
flutter analyze lib/data/epub/epub_metadata_parser.dart
```

Ak vidíš `Undefined name 'encodePng'`, run:
```
flutter pub add image
```

Pridaj import navrch parser súboru:
```dart
import 'package:image/image.dart';
```

- [ ] **Step 9.4: Run test**

```
flutter test test/data/epub/epub_metadata_parser_test.dart
```

Expected: 3 testy pass. Ak `alice.epub` má neočakávaný formát obálky, môžeš dočasne uvoľniť assertion `coverBytes != null` na `coverBytes == null || coverBytes!.length > 100`.

- [ ] **Step 9.5: Verify all + commit & push**

```
flutter analyze
flutter test
git add lib/ test/ pubspec.yaml pubspec.lock
git commit -m "feat(m2): EPUB metadata parser (title, author, language, cover)"
git push
```

---

## Task 10: CoverStorage helper + test

Zapisuje cover bytes do `${ApplicationSupportDir}/EpubReader/covers/<bookId>.png` a vracia cestu. Testovateľná cez `withTempDir`.

**Files:**
- Create: `lib/data/storage/cover_storage.dart`
- Create: `test/data/storage/cover_storage_test.dart`

- [ ] **Step 10.1: Vytvor `test/data/storage/cover_storage_test.dart`**

```dart
import 'dart:io';
import 'dart:typed_data';

import 'package:flutter_test/flutter_test.dart';

import 'package:epubreader/data/storage/cover_storage.dart';

void main() {
  late Directory tmp;
  late CoverStorage storage;

  setUp(() {
    tmp = Directory.systemTemp.createTempSync('cover_storage_test_');
    storage = CoverStorage(baseDir: tmp);
  });

  tearDown(() => tmp.deleteSync(recursive: true));

  test('store writes png and returns absolute path', () async {
    final bytes = Uint8List.fromList(List<int>.generate(200, (i) => i));
    final path = await storage.store('book-1', bytes);

    expect(File(path).existsSync(), isTrue);
    expect(File(path).readAsBytesSync(), bytes);
    expect(path.endsWith('book-1.png'), isTrue);
  });

  test('store overwrites existing cover for same id', () async {
    await storage.store('b', Uint8List.fromList([1, 2, 3]));
    await storage.store('b', Uint8List.fromList([4, 5, 6]));
    final path = await storage.pathFor('b');
    expect(File(path).readAsBytesSync(), [4, 5, 6]);
  });
}
```

- [ ] **Step 10.2: Vytvor `lib/data/storage/cover_storage.dart`**

```dart
import 'dart:io';
import 'dart:typed_data';

import 'package:path_provider/path_provider.dart';

class CoverStorage {
  final Directory baseDir;

  CoverStorage({required this.baseDir});

  static Future<CoverStorage> system() async {
    final support = await getApplicationSupportDirectory();
    final dir = Directory('${support.path}/EpubReader/covers');
    if (!dir.existsSync()) {
      dir.createSync(recursive: true);
    }
    return CoverStorage(baseDir: dir);
  }

  String pathFor(String bookId) => '${baseDir.path}/$bookId.png';

  Future<String> store(String bookId, Uint8List bytes) async {
    if (!baseDir.existsSync()) {
      baseDir.createSync(recursive: true);
    }
    final path = pathFor(bookId);
    await File(path).writeAsBytes(bytes, flush: true);
    return path;
  }
}
```

- [ ] **Step 10.3: Spusti test**

```
flutter test test/data/storage/cover_storage_test.dart
```

Expected: 2 testy pass.

- [ ] **Step 10.4: Verify all + commit & push**

```
flutter analyze
flutter test
git add lib/data/ test/data/
git commit -m "feat(m2): CoverStorage — write covers under ApplicationSupportDir"
git push
```

---

## Task 11: AddBookUseCase + test

Orchestrácia: prijmeme cestu k EPUB súboru → parser extrahuje metadata + cover → optional copy do managed library dir → cover na disk → DB insert. Vracia novovytvorený `BookModel`.

V M2 podporujeme len managed mode (kópia EPUB-u do app data dir). Watched mode pridáme v M5.

**Files:**
- Create: `lib/features/library/add_book_use_case.dart`
- Create: `test/features/library/add_book_use_case_test.dart`

- [ ] **Step 11.1: Vytvor `test/features/library/add_book_use_case_test.dart`**

```dart
import 'dart:io';
import 'dart:typed_data';

import 'package:drift/drift.dart';
import 'package:drift/native.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:epubreader/data/database/app_database.dart';
import 'package:epubreader/data/database/daos/books_dao.dart';
import 'package:epubreader/data/epub/epub_metadata_parser.dart';
import 'package:epubreader/data/storage/cover_storage.dart';
import 'package:epubreader/features/library/add_book_use_case.dart';

void main() {
  late Directory tmp;
  late AppDatabase db;
  late BooksDao booksDao;
  late CoverStorage cover;
  late AddBookUseCase useCase;

  setUp(() {
    tmp = Directory.systemTemp.createTempSync('addbook_uc_');
    db = AppDatabase(DatabaseConnection(NativeDatabase.memory()));
    booksDao = BooksDao(db);
    cover = CoverStorage(baseDir: Directory('${tmp.path}/covers'));
    useCase = AddBookUseCase(
      booksDao: booksDao,
      coverStorage: cover,
      managedLibraryDir: Directory('${tmp.path}/books'),
      parse: _fakeParse,
    );
  });

  tearDown(() async {
    await db.close();
    tmp.deleteSync(recursive: true);
  });

  test('addBook stores file copy, cover, and DB row', () async {
    final src = File('${tmp.path}/orig.epub')..writeAsBytesSync([1, 2, 3, 4]);

    final book = await useCase.addBook(src);

    expect(book.title, 'Fake Title');
    expect(book.author, 'Fake Author');
    expect(book.storageMode, 'managed');
    expect(File(book.filePath).existsSync(), isTrue);
    expect(book.filePath.contains('${tmp.path}/books'), isTrue);
    expect(File(book.coverPath!).existsSync(), isTrue);

    final rows = await booksDao.getAll();
    expect(rows, hasLength(1));
    expect(rows.single.id, book.id);
  });
}

Future<EpubMetadata> _fakeParse(File f) async {
  return EpubMetadata(
    title: 'Fake Title',
    author: 'Fake Author',
    language: 'en',
    coverBytes: Uint8List.fromList(List<int>.generate(150, (i) => i)),
  );
}
```

- [ ] **Step 11.2: Vytvor `lib/features/library/add_book_use_case.dart`**

```dart
import 'dart:io';

import 'package:drift/drift.dart' show Value;

import '../../data/database/app_database.dart';
import '../../data/database/daos/books_dao.dart';
import '../../data/database/tables/books_table.dart';
import '../../data/epub/epub_metadata_parser.dart';
import '../../data/storage/cover_storage.dart';
import '../../domain/models/book.dart';

typedef ParseFn = Future<EpubMetadata> Function(File file);

class AddBookUseCase {
  final BooksDao booksDao;
  final CoverStorage coverStorage;
  final Directory managedLibraryDir;
  final ParseFn parse;

  AddBookUseCase({
    required this.booksDao,
    required this.coverStorage,
    required this.managedLibraryDir,
    ParseFn? parse,
  }) : parse = parse ?? EpubMetadataParser.parse;

  Future<BookModel> addBook(File sourceFile) async {
    final meta = await parse(sourceFile);

    final bookId = _generateBookId(meta);
    if (!managedLibraryDir.existsSync()) {
      managedLibraryDir.createSync(recursive: true);
    }
    final managedPath = '${managedLibraryDir.path}/$bookId.epub';
    await sourceFile.copy(managedPath);

    String? coverPath;
    if (meta.coverBytes != null) {
      coverPath = await coverStorage.store(bookId, meta.coverBytes!);
    }

    final size = await File(managedPath).length();
    final addedAt = DateTime.now();

    final companion = BooksCompanion.insert(
      id: bookId,
      title: meta.title,
      filePath: managedPath,
      storageMode: 'managed',
      addedAt: addedAt,
      author: Value(meta.author),
      language: Value(meta.language),
      coverPath: Value(coverPath),
      fileSizeBytes: Value(size),
    );
    await booksDao.insertBook(companion);

    final row = await booksDao.getById(bookId);
    return BookModel.fromRow(row!);
  }

  String _generateBookId(EpubMetadata meta) {
    final ts = DateTime.now().microsecondsSinceEpoch;
    final slug = meta.title
        .toLowerCase()
        .replaceAll(RegExp(r'[^a-z0-9]+'), '-')
        .replaceAll(RegExp(r'^-|-\$'), '');
    final short = slug.length > 40 ? slug.substring(0, 40) : slug;
    return '$short-$ts';
  }
}
```

- [ ] **Step 11.3: Spusti test**

```
flutter test test/features/library/add_book_use_case_test.dart
```

Expected: 1 test pass.

- [ ] **Step 11.4: Verify all + commit & push**

```
flutter analyze
flutter test
git add lib/ test/
git commit -m "feat(m2): AddBookUseCase orchestrates parse + copy + cover + db insert"
git push
```

---

## Task 12: Riverpod providers

Vytvoríme providery pre AppDatabase, BooksDao, CoverStorage, AddBookUseCase, a streamový provider pre zoznam kníh.

**Files:**
- Create: `lib/features/library/library_providers.dart`

- [ ] **Step 12.1: Vytvor `lib/features/library/library_providers.dart`**

```dart
import 'dart:io';

import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:path_provider/path_provider.dart';

import '../../data/database/app_database.dart';
import '../../data/database/connection.dart';
import '../../data/database/daos/books_dao.dart';
import '../../data/database/daos/settings_kv_dao.dart';
import '../../data/storage/cover_storage.dart';
import '../../domain/models/book.dart';
import 'add_book_use_case.dart';

final appDatabaseProvider = Provider<AppDatabase>((ref) {
  final db = openAppDatabase();
  ref.onDispose(db.close);
  return db;
});

final booksDaoProvider = Provider<BooksDao>(
  (ref) => BooksDao(ref.watch(appDatabaseProvider)),
);

final settingsKvDaoProvider = Provider<SettingsKvDao>(
  (ref) => SettingsKvDao(ref.watch(appDatabaseProvider)),
);

final coverStorageProvider = FutureProvider<CoverStorage>(
  (ref) async => CoverStorage.system(),
);

final managedLibraryDirProvider = FutureProvider<Directory>((ref) async {
  final support = await getApplicationSupportDirectory();
  final dir = Directory('${support.path}/EpubReader/Books');
  if (!dir.existsSync()) {
    dir.createSync(recursive: true);
  }
  return dir;
});

final addBookUseCaseProvider = FutureProvider<AddBookUseCase>((ref) async {
  final coverStorage = await ref.watch(coverStorageProvider.future);
  final managedDir = await ref.watch(managedLibraryDirProvider.future);
  return AddBookUseCase(
    booksDao: ref.watch(booksDaoProvider),
    coverStorage: coverStorage,
    managedLibraryDir: managedDir,
  );
});

final libraryBooksProvider = StreamProvider<List<BookModel>>((ref) {
  final dao = ref.watch(booksDaoProvider);
  return dao.watchAll().map(
        (rows) => rows.map(BookModel.fromRow).toList(growable: false),
      );
});
```

- [ ] **Step 12.2: Verify**

```
flutter analyze
flutter test
```

Expected: pass.

- [ ] **Step 12.3: Commit & push**

```
git add lib/features/
git commit -m "feat(m2): riverpod providers for db, daos, storage, library books stream"
git push
```

---

## Task 13: LibraryScreen UI (grid + list switcher, empty state)

Hlavná obrazovka knižnice. Reaguje na `libraryBooksProvider` stream. Má prepínač **Grid ↔ List** v AppBar. Prázdny stav ukazuje onboarding s tlačidlom „Pridať knihu".

**Files:**
- Create: `lib/features/library/library_book_card.dart`
- Create: `lib/features/library/library_screen.dart`

- [ ] **Step 13.1: Vytvor `lib/features/library/library_book_card.dart`**

```dart
import 'dart:io';

import 'package:flutter/material.dart';
import 'package:flutter_gen/gen_l10n/app_localizations.dart';

import '../../domain/models/book.dart';

class LibraryBookGridCard extends StatelessWidget {
  final BookModel book;
  final VoidCallback onTap;

  const LibraryBookGridCard({
    super.key,
    required this.book,
    required this.onTap,
  });

  @override
  Widget build(BuildContext context) {
    final l = AppLocalizations.of(context)!;
    return InkWell(
      onTap: onTap,
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.stretch,
        children: [
          AspectRatio(
            aspectRatio: 2 / 3,
            child: book.coverPath != null && File(book.coverPath!).existsSync()
                ? Image.file(File(book.coverPath!), fit: BoxFit.cover)
                : Container(
                    color: Theme.of(context).colorScheme.surfaceContainerHighest,
                    alignment: Alignment.center,
                    child: const Icon(Icons.book, size: 48),
                  ),
          ),
          const SizedBox(height: 4),
          Text(book.title,
              maxLines: 2,
              overflow: TextOverflow.ellipsis,
              style: const TextStyle(fontWeight: FontWeight.w600)),
          if (book.author != null)
            Text(book.author!,
                maxLines: 1,
                overflow: TextOverflow.ellipsis,
                style: Theme.of(context).textTheme.bodySmall)
          else
            Text(l.unknownAuthor,
                style: Theme.of(context).textTheme.bodySmall),
        ],
      ),
    );
  }
}

class LibraryBookListTile extends StatelessWidget {
  final BookModel book;
  final VoidCallback onTap;

  const LibraryBookListTile({
    super.key,
    required this.book,
    required this.onTap,
  });

  @override
  Widget build(BuildContext context) {
    final l = AppLocalizations.of(context)!;
    return ListTile(
      onTap: onTap,
      leading: SizedBox(
        width: 40,
        child: book.coverPath != null && File(book.coverPath!).existsSync()
            ? Image.file(File(book.coverPath!), fit: BoxFit.cover)
            : const Icon(Icons.book),
      ),
      title: Text(book.title),
      subtitle: Text(book.author ?? l.unknownAuthor),
    );
  }
}
```

- [ ] **Step 13.2: Vytvor `lib/features/library/library_screen.dart`** (zatiaľ bez addBook flow — len UI + stream)

```dart
import 'package:flutter/material.dart';
import 'package:flutter_gen/gen_l10n/app_localizations.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

import 'library_book_card.dart';
import 'library_providers.dart';

enum LibraryViewMode { grid, list }

final libraryViewModeProvider =
    StateProvider<LibraryViewMode>((_) => LibraryViewMode.grid);

class LibraryScreen extends ConsumerWidget {
  const LibraryScreen({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final l = AppLocalizations.of(context)!;
    final viewMode = ref.watch(libraryViewModeProvider);
    final booksAsync = ref.watch(libraryBooksProvider);

    return Scaffold(
      appBar: AppBar(
        title: Text(l.libraryTitle),
        actions: [
          IconButton(
            tooltip: viewMode == LibraryViewMode.grid ? l.viewList : l.viewGrid,
            icon: Icon(viewMode == LibraryViewMode.grid
                ? Icons.view_list
                : Icons.grid_view),
            onPressed: () => ref.read(libraryViewModeProvider.notifier).state =
                viewMode == LibraryViewMode.grid
                    ? LibraryViewMode.list
                    : LibraryViewMode.grid,
          ),
        ],
      ),
      body: booksAsync.when(
        loading: () => const Center(child: CircularProgressIndicator()),
        error: (e, _) => Center(child: Text('$e')),
        data: (books) {
          if (books.isEmpty) {
            return Center(
              child: Padding(
                padding: const EdgeInsets.all(24),
                child: Text(l.libraryEmpty, textAlign: TextAlign.center),
              ),
            );
          }
          if (viewMode == LibraryViewMode.grid) {
            return GridView.builder(
              padding: const EdgeInsets.all(16),
              gridDelegate: const SliverGridDelegateWithMaxCrossAxisExtent(
                maxCrossAxisExtent: 180,
                childAspectRatio: 2 / 3.4,
                mainAxisSpacing: 16,
                crossAxisSpacing: 16,
              ),
              itemCount: books.length,
              itemBuilder: (_, i) => LibraryBookGridCard(
                book: books[i],
                onTap: () => Navigator.of(context).pushNamed(
                  '/reader',
                  arguments: books[i].id,
                ),
              ),
            );
          }
          return ListView.builder(
            itemCount: books.length,
            itemBuilder: (_, i) => LibraryBookListTile(
              book: books[i],
              onTap: () => Navigator.of(context).pushNamed(
                '/reader',
                arguments: books[i].id,
              ),
            ),
          );
        },
      ),
    );
  }
}
```

- [ ] **Step 13.3: Verify analyze**

```
flutter analyze
```

Expected: no issues. (Test by si zatiaľ vyžadoval wiring v main.dart — to spravíme v Task 16.)

- [ ] **Step 13.4: Commit & push**

```
git add lib/features/
git commit -m "feat(m2): LibraryScreen with grid/list switcher and empty state"
git push
```

---

## Task 14: AddBook flow — file picker → use case → state update

Pridáme **FAB** (FloatingActionButton) na LibraryScreen ktorý otvorí file picker, zavolá `AddBookUseCase`, a vyzve LoadingNotifier počas behu.

**Files:**
- Modify: `lib/features/library/library_screen.dart`

- [ ] **Step 14.1: Pridať FAB + addBook handler do `library_screen.dart`**

Aktualizuj `library_screen.dart` — pridaj importy a FAB:

```dart
import 'dart:io';

import 'package:file_picker/file_picker.dart';
// (zachovaj ostatné importy)
```

V `Scaffold(...)` pridaj `floatingActionButton`:

```dart
      floatingActionButton: FloatingActionButton.extended(
        icon: const Icon(Icons.add),
        label: Text(l.addBook),
        onPressed: () => _pickAndAdd(context, ref),
      ),
```

A na koniec class file pridaj method (mimo `build`):

```dart
Future<void> _pickAndAdd(BuildContext context, WidgetRef ref) async {
  final result = await FilePicker.pickFiles(
    type: FileType.custom,
    allowedExtensions: ['epub'],
    allowMultiple: false,
    dialogTitle: 'Vyber EPUB súbor',
  );
  if (result == null || result.files.isEmpty) return;
  final path = result.files.first.path;
  if (path == null) return;

  final messenger = ScaffoldMessenger.of(context);
  final l = AppLocalizations.of(context)!;
  try {
    final useCase = await ref.read(addBookUseCaseProvider.future);
    await useCase.addBook(File(path));
  } catch (e) {
    messenger.showSnackBar(SnackBar(content: Text('${l.errorOpenEpub} ($e)')));
  }
}
```

(Použiť `WidgetRef ref` parameter v `_pickAndAdd` znamená že volajúce miesto musí byť v `ConsumerWidget.build` kde `ref` máme. To už je.)

- [ ] **Step 14.2: Verify analyze**

```
flutter analyze
```

Expected: clean.

- [ ] **Step 14.3: Commit & push**

```
git add lib/features/
git commit -m "feat(m2): library add-book FAB wired to file picker + use case"
git push
```

---

## Task 15: ReaderStubScreen

Provizórna obrazovka — ukáže názov a autora, info že reálne čítanie príde v M3.

**Files:**
- Create: `lib/features/reader/reader_stub_screen.dart`

- [ ] **Step 15.1: Vytvor `lib/features/reader/reader_stub_screen.dart`**

```dart
import 'package:flutter/material.dart';
import 'package:flutter_gen/gen_l10n/app_localizations.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

import '../library/library_providers.dart';

class ReaderStubScreen extends ConsumerWidget {
  final String bookId;
  const ReaderStubScreen({super.key, required this.bookId});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final l = AppLocalizations.of(context)!;
    final booksAsync = ref.watch(libraryBooksProvider);

    return Scaffold(
      appBar: AppBar(title: Text(l.readerStubTitle)),
      body: booksAsync.when(
        loading: () => const Center(child: CircularProgressIndicator()),
        error: (e, _) => Center(child: Text('$e')),
        data: (books) {
          final book = books.where((b) => b.id == bookId).cast<dynamic>().firstWhere(
                (_) => true,
                orElse: () => null,
              );
          if (book == null) {
            return const Center(child: Text('Book not found'));
          }
          return Padding(
            padding: const EdgeInsets.all(24),
            child: Column(
              crossAxisAlignment: CrossAxisAlignment.start,
              children: [
                Text(book.title, style: Theme.of(context).textTheme.headlineMedium),
                const SizedBox(height: 8),
                Text(book.author ?? l.unknownAuthor,
                    style: Theme.of(context).textTheme.titleMedium),
                const SizedBox(height: 24),
                Text(l.readerStubBody),
              ],
            ),
          );
        },
      ),
    );
  }
}
```

- [ ] **Step 15.2: Verify analyze**

```
flutter analyze
```

- [ ] **Step 15.3: Commit & push**

```
git add lib/features/
git commit -m "feat(m2): ReaderStubScreen — placeholder showing book info"
git push
```

---

## Task 16: Wire navigation + replace spike menu + cleanup

Posledný task: nahradíme dočasné spike menu reálnym `LibraryScreen` ako home a vyčistíme staré spike súbory.

**Files:**
- Modify: `lib/app.dart`
- Modify: `lib/main.dart` (žiadne — main už je správny)
- Delete: `lib/spike/spike_menu.dart`
- Delete: `lib/spike/epub_view_spike.dart`
- Delete: `lib/shared/fixture_picker.dart`
- Modify: `test/widget_test.dart`

- [ ] **Step 16.1: Nahraď `lib/app.dart`** kompletne:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_gen/gen_l10n/app_localizations.dart';
import 'package:flutter_localizations/flutter_localizations.dart';

import 'features/library/library_screen.dart';
import 'features/reader/reader_stub_screen.dart';

class EpubReaderApp extends StatelessWidget {
  const EpubReaderApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      onGenerateTitle: (ctx) => AppLocalizations.of(ctx)!.appTitle,
      localizationsDelegates: AppLocalizations.localizationsDelegates,
      supportedLocales: AppLocalizations.supportedLocales,
      theme: ThemeData(
        colorScheme: ColorScheme.fromSeed(seedColor: Colors.indigo),
        useMaterial3: true,
      ),
      home: const LibraryScreen(),
      onGenerateRoute: (settings) {
        if (settings.name == '/reader') {
          final bookId = settings.arguments as String;
          return MaterialPageRoute(
            builder: (_) => ReaderStubScreen(bookId: bookId),
          );
        }
        return null;
      },
      onUnknownRoute: (settings) => MaterialPageRoute(
        builder: (_) => Scaffold(
          appBar: AppBar(title: const Text('Not found')),
          body: Center(child: Text('Route: ${settings.name}')),
        ),
      ),
    );
  }
}
```

- [ ] **Step 16.2: Zmaž spike súbory**

```
rm lib/spike/spike_menu.dart
rm lib/spike/epub_view_spike.dart
rmdir lib/spike
rm lib/shared/fixture_picker.dart
rmdir lib/shared
```

(Ak `rmdir` zlyhá lebo priečinok nie je prázdny, pozri čo v ňom je — možno tam zostalo niečo neočakávané.)

- [ ] **Step 16.3: Aktualizuj `test/widget_test.dart`**

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:epubreader/app.dart';

void main() {
  testWidgets('Library home shows library title in app bar', (tester) async {
    await tester.pumpWidget(const ProviderScope(child: EpubReaderApp()));
    await tester.pump();

    // Library title comes from ARB; we just check the AppBar exists with some text.
    expect(find.byType(AppBar), findsOneWidget);
  });
}
```

- [ ] **Step 16.4: Verify all**

```
flutter analyze
flutter test
flutter build windows --debug
```

Expected: analyze clean, test passes, Windows build succeeds.

- [ ] **Step 16.5: Manuálny end-to-end test (necháme používateľa)**

Spusti `flutter run -d windows`. Manuálne over:
1. Otvorí sa Library obrazovka, prázdna, s textom „Knižnica je prázdna…".
2. Klik na FAB „Pridať knihu" → file picker → vyber `test/fixtures/alice.epub`.
3. Po pár sekundách sa kniha objaví v knižnici s obálkou.
4. Klik na knihu → otvorí sa Reader stub s názvom a autorom.
5. Návrat späť, prepnúť na list view (ikona vpravo hore) → kniha sa zobrazí ako riadok.
6. Zatvoriť appku, znova spustiť → kniha tam stále je (perzistencia funguje).

Ak niečo z toho zlyhá, report status `DONE_WITH_CONCERNS` a popíš čo presne sa stalo.

- [ ] **Step 16.6: Commit & push**

```
git add -A
git status
```

Verify staged: modified app.dart, deleted spike files, modified widget_test. Žiadne junk súbory.

```
git commit -m "feat(m2): replace spike menu with LibraryScreen home + Reader stub route"
git push
```

---

## Akceptačné kritériá M2

- [ ] `flutter analyze` clean
- [ ] `flutter test` passes (všetky unit + integration testy)
- [ ] `flutter build windows --debug` builds successfully
- [ ] Manuálny e2e (Step 16.5) funguje: pridanie knihy → zobrazenie → klik → reader stub → reštart → kniha stále tam
- [ ] Žiadne hardcoded UI stringy mimo `lib/l10n/*.arb` v novom kóde
- [ ] PR description / commit referencuje phase-1-mvp spec sekcie §5.1 a §6

## Mimo rozsah (do neskorších milníkov)

- Pridanie priečinka batch, drag & drop → M5
- Watched folders mode UX → M5
- Settings stránky (Vzhľad, Čítanie, Pokročilé) → M5
- Reálne čítanie EPUB → M3, M4
- Home s 3 sekciami („Pokračovať", „Naposledy pridané", „Knižnica") → M5
- Manuálna úprava metadát knihy → Fáza 4

## Po M2

Vytvorenie plánu pre **M3 (Reader v1 — scroll mode)** sa robí znova cez `superpowers:writing-plans`.
